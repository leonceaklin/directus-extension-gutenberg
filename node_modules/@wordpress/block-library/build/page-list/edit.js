"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = PageListEdit;

var _element = require("@wordpress/element");

var _classnames = _interopRequireDefault(require("classnames"));

var _blocks = require("@wordpress/blocks");

var _blockEditor = require("@wordpress/block-editor");

var _components = require("@wordpress/components");

var _i18n = require("@wordpress/i18n");

var _coreData = require("@wordpress/core-data");

var _data = require("@wordpress/data");

var _convertToLinksModal = _interopRequireDefault(require("./convert-to-links-modal"));

var _convertToNavigationLinks = require("./convert-to-navigation-links");

var _constants = require("./constants");

/**
 * External dependencies
 */

/**
 * WordPress dependencies
 */

/**
 * Internal dependencies
 */
// We only show the edit option when page count is <= MAX_PAGE_COUNT
// Performance of Navigation Links is not good past this value.
const MAX_PAGE_COUNT = 100;

const NOOP = () => {};

function PageListEdit(_ref) {
  var _context$style;

  let {
    context,
    clientId,
    attributes,
    setAttributes
  } = _ref;
  const {
    parentPageID
  } = attributes;
  const [pages] = useGetPages();
  const {
    pagesByParentId,
    totalPages,
    hasResolvedPages
  } = usePageData();
  const isNavigationChild = ('showSubmenuIcon' in context);
  const allowConvertToLinks = isNavigationChild && totalPages <= MAX_PAGE_COUNT;
  const [isOpen, setOpen] = (0, _element.useState)(false);

  const openModal = () => setOpen(true);

  const closeModal = () => setOpen(false);

  const blockProps = (0, _blockEditor.useBlockProps)({
    className: (0, _classnames.default)('wp-block-page-list', {
      'has-text-color': !!context.textColor,
      [(0, _blockEditor.getColorClassName)('color', context.textColor)]: !!context.textColor,
      'has-background': !!context.backgroundColor,
      [(0, _blockEditor.getColorClassName)('background-color', context.backgroundColor)]: !!context.backgroundColor
    }),
    style: { ...((_context$style = context.style) === null || _context$style === void 0 ? void 0 : _context$style.color)
    }
  });

  const getBlockList = function () {
    let parentId = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : parentPageID;
    const childPages = pagesByParentId.get(parentId);

    if (!(childPages !== null && childPages !== void 0 && childPages.length)) {
      return [];
    }

    return childPages.reduce((template, page) => {
      var _page$title, _page$title2;

      const hasChildren = pagesByParentId.has(page.id);
      const pageProps = {
        id: page.id,
        label: (_page$title = page.title) === null || _page$title === void 0 ? void 0 : _page$title.rendered,
        title: (_page$title2 = page.title) === null || _page$title2 === void 0 ? void 0 : _page$title2.rendered,
        link: page.url,
        hasChildren
      };
      let item = null;
      const children = getBlockList(page.id);
      item = (0, _blocks.createBlock)('core/page-list-item', pageProps, children);
      template.push(item);
      return template;
    }, []);
  };

  const makePagesTree = function () {
    let parentId = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
    let level = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
    const childPages = pagesByParentId.get(parentId);

    if (!(childPages !== null && childPages !== void 0 && childPages.length)) {
      return [];
    }

    return childPages.reduce((tree, page) => {
      const hasChildren = pagesByParentId.has(page.id);
      const item = {
        value: page.id,
        label: '— '.repeat(level) + page.title.rendered,
        rawName: page.title.rendered
      };
      tree.push(item);

      if (hasChildren) {
        tree.push(...makePagesTree(page.id, level + 1));
      }

      return tree;
    }, []);
  };

  const pagesTree = (0, _element.useMemo)(makePagesTree, [pagesByParentId]);
  const blockList = (0, _element.useMemo)(getBlockList, [pagesByParentId, parentPageID]);
  const innerBlocksProps = (0, _blockEditor.useInnerBlocksProps)(blockProps, {
    allowedBlocks: ['core/page-list-item'],
    renderAppender: false,
    __unstableDisableDropZone: true,
    templateLock: 'all',
    onInput: NOOP,
    onChange: NOOP,
    value: blockList
  });

  const getBlockContent = () => {
    if (!hasResolvedPages) {
      return (0, _element.createElement)("div", blockProps, (0, _element.createElement)(_components.Spinner, null));
    }

    if (totalPages === null) {
      return (0, _element.createElement)("div", blockProps, (0, _element.createElement)(_components.Notice, {
        status: 'warning',
        isDismissible: false
      }, (0, _i18n.__)('Page List: Cannot retrieve Pages.')));
    }

    if (totalPages === 0) {
      return (0, _element.createElement)("div", blockProps, (0, _element.createElement)(_components.Notice, {
        status: 'info',
        isDismissible: false
      }, (0, _i18n.__)('Page List: Cannot retrieve Pages.')));
    }

    if (blockList.length === 0) {
      const parentPageDetails = pages && pages.find(page => page.id === parentPageID);
      return (0, _element.createElement)("div", blockProps, (0, _element.createElement)(_blockEditor.Warning, null, (0, _i18n.sprintf)( // translators: %s: Page title.
      (0, _i18n.__)('"%s" page has no children.'), parentPageDetails.title.rendered)));
    }

    if (totalPages > 0) {
      return (0, _element.createElement)("ul", innerBlocksProps);
    }
  };

  const {
    replaceBlock,
    selectBlock
  } = (0, _data.useDispatch)(_blockEditor.store);
  const {
    parentNavBlockClientId,
    isNested
  } = (0, _data.useSelect)(select => {
    const {
      getSelectedBlockClientId,
      getBlockParentsByBlockName
    } = select(_blockEditor.store);

    const _selectedBlockClientId = getSelectedBlockClientId();

    return {
      parentNavBlockClientId: getBlockParentsByBlockName(_selectedBlockClientId, 'core/navigation', true)[0],
      isNested: getBlockParentsByBlockName(clientId, 'core/navigation-submenu', true).length > 0
    };
  }, [clientId]);
  (0, _element.useEffect)(() => {
    setAttributes({
      isNested
    });
  }, [isNested]);
  return (0, _element.createElement)(_element.Fragment, null, (0, _element.createElement)(_blockEditor.InspectorControls, null, isNavigationChild && (pages === null || pages === void 0 ? void 0 : pages.length) > 0 && (0, _element.createElement)(_components.PanelBody, {
    title: (0, _i18n.__)('Customize this menu')
  }, (0, _element.createElement)("p", null, _constants.convertDescription), (0, _element.createElement)(_components.Button, {
    variant: "primary",
    disabled: !hasResolvedPages,
    onClick: () => {
      const navigationLinks = (0, _convertToNavigationLinks.convertToNavigationLinks)(pages); // Replace the Page List block with the Navigation Links.

      replaceBlock(clientId, navigationLinks); // Select the Navigation block to reveal the changes.

      selectBlock(parentNavBlockClientId);
    }
  }, (0, _i18n.__)('Customize'))), pagesTree.length > 0 && (0, _element.createElement)(_components.PanelBody, null, (0, _element.createElement)(_components.ComboboxControl, {
    className: "editor-page-attributes__parent",
    label: (0, _i18n.__)('Parent page'),
    value: parentPageID,
    options: pagesTree,
    onChange: value => setAttributes({
      parentPageID: value !== null && value !== void 0 ? value : 0
    }),
    help: (0, _i18n.__)('Choose a page to show only its subpages.')
  }))), allowConvertToLinks && totalPages > 0 && (0, _element.createElement)(_blockEditor.BlockControls, {
    group: "other"
  }, (0, _element.createElement)(_components.ToolbarButton, {
    title: (0, _i18n.__)('Edit'),
    onClick: openModal
  }, (0, _i18n.__)('Edit'))), allowConvertToLinks && isOpen && (0, _element.createElement)(_convertToLinksModal.default, {
    onClose: closeModal,
    clientId: clientId
  }), getBlockContent());
}

function useGetPages() {
  const {
    records: pages,
    hasResolved: hasResolvedPages
  } = (0, _coreData.useEntityRecords)('postType', 'page', {
    orderby: 'menu_order',
    order: 'asc',
    _fields: ['id', 'link', 'parent', 'title', 'menu_order'],
    per_page: -1,
    context: 'view'
  });
  return [pages, hasResolvedPages];
}

function usePageData() {
  let pageId = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
  const [pages, hasResolvedPages] = useGetPages();
  return (0, _element.useMemo)(() => {
    var _pages$length;

    // TODO: Once the REST API supports passing multiple values to
    // 'orderby', this can be removed.
    // https://core.trac.wordpress.org/ticket/39037
    const sortedPages = [...(pages !== null && pages !== void 0 ? pages : [])].sort((a, b) => {
      if (a.menu_order === b.menu_order) {
        return a.title.rendered.localeCompare(b.title.rendered);
      }

      return a.menu_order - b.menu_order;
    });
    const pagesByParentId = sortedPages.reduce((accumulator, page) => {
      const {
        parent
      } = page;

      if (accumulator.has(parent)) {
        accumulator.get(parent).push(page);
      } else {
        accumulator.set(parent, [page]);
      }

      return accumulator;
    }, new Map());
    return {
      pagesByParentId,
      hasResolvedPages,
      totalPages: (_pages$length = pages === null || pages === void 0 ? void 0 : pages.length) !== null && _pages$length !== void 0 ? _pages$length : null
    };
  }, [pageId, pages, hasResolvedPages]);
}
//# sourceMappingURL=edit.js.map