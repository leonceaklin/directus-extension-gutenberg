"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = useInspectorControlsTabs;

var _components = require("@wordpress/components");

var _data = require("@wordpress/data");

var _groups = _interopRequireDefault(require("../inspector-controls/groups"));

var _useIsListViewTabDisabled = _interopRequireDefault(require("./use-is-list-view-tab-disabled"));

var _inspectorControls = require("../inspector-controls");

var _utils = require("./utils");

var _store = require("../../store");

/**
 * WordPress dependencies
 */

/**
 * Internal dependencies
 */
const EMPTY_ARRAY = [];

function getShowTabs(blockName) {
  var _window;

  let tabSettings = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

  // Don't allow settings to force the display of tabs if the block inspector
  // tabs experiment hasn't been opted into.
  if (!((_window = window) !== null && _window !== void 0 && _window.__experimentalEnableBlockInspectorTabs)) {
    return false;
  } // Block specific setting takes precedence over generic default.


  if (tabSettings[blockName] !== undefined) {
    return tabSettings[blockName];
  } // Use generic default if set over the Gutenberg experiment option.


  if (tabSettings.default !== undefined) {
    return tabSettings.default;
  }

  return true;
}

function useInspectorControlsTabs(blockName) {
  const tabs = [];
  const {
    border: borderGroup,
    color: colorGroup,
    default: defaultGroup,
    dimensions: dimensionsGroup,
    list: listGroup,
    typography: typographyGroup
  } = _groups.default; // List View Tab: If there are any fills for the list group add that tab.

  const listViewDisabled = (0, _useIsListViewTabDisabled.default)(blockName);
  const listFills = (0, _components.__experimentalUseSlotFills)(listGroup.Slot.__unstableName);

  if (!listViewDisabled && !!listFills && listFills.length) {
    tabs.push(_utils.TAB_LIST_VIEW);
  } // Styles Tab: Add this tab if there are any fills for block supports
  // e.g. border, color, spacing, typography, etc.


  const styleFills = [...((0, _components.__experimentalUseSlotFills)(borderGroup.Slot.__unstableName) || []), ...((0, _components.__experimentalUseSlotFills)(colorGroup.Slot.__unstableName) || []), ...((0, _components.__experimentalUseSlotFills)(dimensionsGroup.Slot.__unstableName) || []), ...((0, _components.__experimentalUseSlotFills)(typographyGroup.Slot.__unstableName) || [])];

  if (styleFills.length) {
    tabs.push(_utils.TAB_STYLES);
  } // Settings Tab: If there are any fills for the general InspectorControls
  // or Advanced Controls slot, then add this tab.


  const settingsFills = [...((0, _components.__experimentalUseSlotFills)(defaultGroup.Slot.__unstableName) || []), ...((0, _components.__experimentalUseSlotFills)(_inspectorControls.InspectorAdvancedControls.slotName) || [])];

  if (settingsFills.length) {
    tabs.push(_utils.TAB_SETTINGS);
  }

  const tabSettings = (0, _data.useSelect)(select => {
    return select(_store.store).getSettings().__experimentalBlockInspectorTabs;
  }, []);
  const showTabs = getShowTabs(blockName, tabSettings);
  return showTabs ? tabs : EMPTY_ARRAY;
}
//# sourceMappingURL=use-inspector-controls-tabs.js.map